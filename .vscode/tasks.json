{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "dev env",
            "type": "shell",
            "command": "docker compose up --build --remove-orphans",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}